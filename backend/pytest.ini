[pytest]
testpaths = tests
# conftest.py at the backend root puts this directory on sys.path, so tests
# import `app.*` exactly the way `uvicorn app.main:app` does.
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
